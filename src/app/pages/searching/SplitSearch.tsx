import JumpSearch from "./JumpSearch";

export default function SplitSearch() {
  return (
    <JumpSearch
      title="Split Search"
      description="Split Search is presented here as a block-based search visualizer: split the sorted array into chunks, jump between block boundaries, then scan locally inside the chosen segment."
    />
  );
}
